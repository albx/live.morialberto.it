query GetScheduledStreamings {
  streamings (
    orderBy: {
      ScheduleDate: DESC,
      StartingTime: DESC,
      EndingTime: DESC 
    }, first: 4) {
    items {
      Id,
      Title,
      Slug,
      ScheduleDate,
      StartingTime,
      EndingTime
    }
  }
}